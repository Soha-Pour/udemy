#integer (the "L" makes the value an integer. Without it, R stores the variable as "double.")
x <- 2L
typeof(x)

#double 
y <- 2.5
typeof(y)

#complex
z <- 3 + 2i 
typeof(z)

#character
a <- "h"
typeof(a)

#logical 
q <- TRUE
typeof(q)
